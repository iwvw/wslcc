<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WSLCC.App.Pages.ActivityPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:WSLCC.Data"
    xmlns:vm="using:WSLCC.App.ViewModels"
    mc:Ignorable="d"
    x:DataType="vm:ActivityViewModel">

    <Grid Padding="32,24,32,24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

<Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock
                Style="{StaticResource TitleTextBlockStyle}"
                Text="活动"
                VerticalAlignment="Center" />
            <StackPanel
                Grid.Column="1"
                Orientation="Horizontal"
                Spacing="8"
                VerticalAlignment="Center">
                <Button
                    Command="{x:Bind ViewModel.RefreshCommand}"
                    Content="刷新" />
                <Button
                    Command="{x:Bind ViewModel.ClearAuditCommand}"
                    Content="清空审计日志" />
            </StackPanel>
        </Grid>

        <InfoBar
            Grid.Row="1"
            IsClosable="True"
            IsOpen="{x:Bind ViewModel.HasError, Mode=OneWay}"
            Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
            Severity="Error"
            Title="操作失败" />

        <TextBlock
            Grid.Row="2"
            Style="{StaticResource SubtitleTextBlockStyle}"
            Text="操作审计" />

        <ListView
            Grid.Row="3"
            ItemsSource="{x:Bind ViewModel.AuditEntries, Mode=OneWay}"
            SelectionMode="None">
<ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:AuditLogItemViewModel">
                    <Grid ColumnSpacing="16" Padding="0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160" />
                            <ColumnDefinition Width="140" />
                            <ColumnDefinition Width="160" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="70" />
                            <ColumnDefinition Width="90" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Style="{StaticResource BodyTextBlockStyle}" Text="{x:Bind TimeText, Mode=OneWay}" />
                        <TextBlock Grid.Column="1" Text="{x:Bind Category, Mode=OneWay}" />
                        <TextBlock Grid.Column="2" Text="{x:Bind Action, Mode=OneWay}" />
                        <TextBlock Grid.Column="3" IsTextSelectionEnabled="True" Text="{x:Bind Detail, Mode=OneWay}" />
                        <TextBlock
                            Grid.Column="4"
                            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                            Text="{x:Bind Result, Mode=OneWay}" />
                        <TextBlock Grid.Column="5" Text="{x:Bind DurationText, Mode=OneWay}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock
            Grid.Row="4"
            Style="{StaticResource SubtitleTextBlockStyle}"
            Text="镜像拉取历史" />

        <ListView
            Grid.Row="5"
            ItemsSource="{x:Bind ViewModel.PullEntries, Mode=OneWay}"
            SelectionMode="None">
<ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:PullHistoryItemViewModel">
                    <Grid ColumnSpacing="16" Padding="0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="90" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Style="{StaticResource BodyTextBlockStyle}" Text="{x:Bind TimeText, Mode=OneWay}" />
                        <TextBlock Grid.Column="1" IsTextSelectionEnabled="True" Text="{x:Bind ImageRef, Mode=OneWay}" />
                        <TextBlock
                            Grid.Column="2"
                            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                            Text="{x:Bind Status, Mode=OneWay}" />
                        <TextBlock Grid.Column="3" IsTextSelectionEnabled="True" Text="{x:Bind Error, Mode=OneWay}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>

