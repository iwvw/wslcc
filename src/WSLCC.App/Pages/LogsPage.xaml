<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WSLCC.App.Pages.LogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:WSLCC.App.ViewModels"
    mc:Ignorable="d"
    x:DataType="vm:LogsViewModel">

    <Grid Padding="32,24,32,24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock
                Style="{StaticResource TitleTextBlockStyle}"
                Text="容器日志"
                VerticalAlignment="Center" />
            <StackPanel
                Grid.Column="1"
                Orientation="Horizontal"
                Spacing="8"
                VerticalAlignment="Center">
                <Button
                    Command="{x:Bind ViewModel.RefreshCommand}"
                    Content="刷新" />
            </StackPanel>
        </Grid>

        <StackPanel
            Grid.Row="1"
            Orientation="Horizontal"
            Spacing="8">
            <ComboBox
                Width="280"
                VerticalAlignment="Center"
                ItemsSource="{x:Bind ViewModel.ContainerNames, Mode=OneWay}"
                PlaceholderText="选择容器"
                SelectedItem="{x:Bind ViewModel.SelectedContainerName, Mode=TwoWay}" />
            <Button
                VerticalAlignment="Center"
                Command="{x:Bind ViewModel.LoadLogsCommand}"
                Content="加载日志" />
        </StackPanel>

        <InfoBar
            Grid.Row="2"
            IsClosable="True"
            IsOpen="{x:Bind ViewModel.HasError, Mode=OneWay}"
            Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
            Severity="Warning"
            Title="日志不可用" />

        <TextBox
            Grid.Row="3"
            AcceptsReturn="True"
            FontFamily="Consolas"
            IsReadOnly="True"
            Text="{x:Bind ViewModel.LogText, Mode=OneWay}"
            TextWrapping="NoWrap" />
    </Grid>
</Page>
